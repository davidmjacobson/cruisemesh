package com.cruisemesh.app.mesh

import android.content.Context

/** Release stub: the same-LAN diagnostic is deliberately absent from production builds. */
internal class SameLanProbeManager(@Suppress("UNUSED_PARAMETER") context: Context) {
    fun start() = Unit
    fun stop() = Unit
}
