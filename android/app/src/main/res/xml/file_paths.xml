<?xml version="1.0" encoding="utf-8"?>
<paths>
    <cache-path name="camera" path="camera/" />
    <cache-path name="voice" path="voice/" />
    <!-- On-device log capture (DebugFileLog; opt-in outside debug builds), shared via the sheet. -->
    <external-files-path name="logs" path="logs/" />
</paths>
