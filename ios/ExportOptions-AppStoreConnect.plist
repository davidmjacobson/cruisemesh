<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Export options for the automated TestFlight release (ios-release.yml).

  MANUAL signing: the workflow imports a dedicated CI distribution certificate
  and this named provisioning profile ("CruiseMesh CI App Store") into a
  throwaway keychain, and xcodebuild signs the export entirely locally. No
  -allowProvisioningUpdates, so nothing contacts Apple's provisioning portal at
  release time (that portal's intermittent 502/504s are exactly what this
  avoids). The provisioningProfiles name below must match the profile stored in
  the IOS_PROVISION_PROFILE_B64 secret. The sibling ExportOptions-TestFlight.plist
  is the older hand-driven variant.

  manageAppVersionAndBuildNumber is false so the CFBundleVersion the workflow
  stamps in (epoch seconds) is preserved rather than auto-managed by Xcode.
-->
<plist version="1.0">
<dict>
	<key>method</key>
	<string>app-store-connect</string>
	<key>teamID</key>
	<string>DDS64SNDZH</string>
	<key>signingStyle</key>
	<string>manual</string>
	<key>provisioningProfiles</key>
	<dict>
		<key>com.cruisemesh.app</key>
		<string>CruiseMesh CI App Store</string>
	</dict>
	<key>manageAppVersionAndBuildNumber</key>
	<false/>
	<key>uploadSymbols</key>
	<true/>
	<key>stripSwiftSymbols</key>
	<true/>
</dict>
</plist>
